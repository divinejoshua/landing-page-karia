To view the gym landing page, simply open the `gym.html` file in your web browser.

You can do this by:
1. Navigating to the project directory in your file explorer.
2. Double-clicking the `gym.html` file.

This will open the page locally in your default web browser.